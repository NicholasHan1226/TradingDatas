# Sentiment read-model projection sources.
#
# /sentiment derives its rows from market_events whose event_type is listed
# below.  Add new event types only after they have a SharedSignals collector
# that writes directly to market_events; do not restore old RSS/CSV bridges.

sentiment_event_types:
  - sentiment
  - major_news
  - news
  - cctv_news
